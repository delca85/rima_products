{
  "name": "rima-products",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild:server": "yarn --cwd server/ install",
    "prebuild:client": "yarn --cwd client/ install",
    "build:client": "yarn --cwd client/ build",
    "build:server": "yarn --cwd server/ bundle",
    "start:client": "yarn --cwd client/ start",
    "start:server": "yarn --cwd server/ start",
    "build": "yarn build:client && yarn build:server"
  }
}

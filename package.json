{
  "name": "rima-products",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild:server": "yarn install --cwd server/",
    "prebuild:client": "yarn install --cwd client/",
    "build:client": "yarn --cwd client/ build",
    "build:server": "yarn --cwd server/ bundle",
    "start:client": "yarn --cwd client/ start",
    "start:server": "yarn --cwd server/ start"
  }
}

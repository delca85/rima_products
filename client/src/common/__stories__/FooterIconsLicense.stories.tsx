import { default as React } from 'react';
import { IRimaComponentProps } from '../../types/component.types';
import FooterIconsLicense from '../FooterIconsLicense';

export default {
  title: 'FooterIconsLicense',
  component: FooterIconsLicense,
};

export const FooterIconsLicenseDefault: React.FunctionComponent<IRimaComponentProps> = () => (
  <FooterIconsLicense />
);

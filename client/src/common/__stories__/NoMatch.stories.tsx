import React from 'react';
import { IRimaComponentProps } from '../../types/component.types';
import NoMatch from '../NoMatch';

export default {
  title: 'NoMatch',
  component: NoMatch,
};

export const NoMatchDefault: React.FunctionComponent<IRimaComponentProps> = () => <NoMatch />;

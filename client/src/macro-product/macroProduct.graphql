query Parts($id: Int!) {
  part(id: $id) {
    id
    name
    thumb
    subparts {
      id
      name
      thumb
      quantity
      subparts {
        id
        name
        thumb
        quantity
      }
    }
  }
}
